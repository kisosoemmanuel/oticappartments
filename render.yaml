services:
  - type: web
    name: otic-apartments
    runtime: node
    plan: free
    region: frankfurt
    numInstances: 1
    autoDeployTrigger: checksPass
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_PATH
        value: /opt/render/project/src/runtime/data.sqlite
      - key: BACKUP_DIR
        value: /opt/render/project/src/runtime/backups
      - key: UPLOAD_DIR
        value: /opt/render/project/src/runtime/uploads
      - key: APP_BASE_URL
        sync: false
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: BACKUP_SECRET
        generateValue: true
